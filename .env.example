# Optional: AI explanation. Without this, the app uses a deterministic local explanation.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Fare providers. The first version includes clean adapter boundaries and demo fallback data.
AMADEUS_CLIENT_ID=
AMADEUS_CLIENT_SECRET=
DUFFEL_ACCESS_TOKEN=
KIWI_API_KEY=
SKYSCANNER_API_KEY=
